import { Injectable } from '@angular/core';
//import { Socket } from 'ngx-socket-io';

@Injectable({
  providedIn: 'root'
})
export class DailyService {
 // currentDaily = this.socket.fromEvent<Document>('daily');
  //dailys = this.socket.fromEvent<[]>('dailys');

 // constructor(private socket: Socket) { }

  /* getDaily(id: string) {
    this.socket.emit('getDaily', id);
  } */

  

 

 
}